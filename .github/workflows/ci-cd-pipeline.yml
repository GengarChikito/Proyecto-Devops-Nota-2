name: CI/CD Microservicio Node.js

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  DOCKER_IMAGE_NAME: mi-microservicio-node
  # ✅ CORREGIDO: Usar tu nombre exacto de GitHub (case-sensitive)
  DOCKER_USER: GengarChikito
  DOCKER_REGISTRY: ghcr.io

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write 
      packages: write
      
    steps:
      - name: ⬇️ Checkout del Código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ⚙️ Configurar Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Instalar dependencias de forma robusta
        run: |
          npm cache clean --force
          npm install --force

      - name: 🛠️ Asignar permisos de ejecución a Jest
        run: chmod +x node_modules/.bin/jest

      - name: 🧪 Ejecutar pruebas y generar reportes de calidad
        run: npm test

      # 🛡️ Análisis de Seguridad de Snyk
      - name: 🛡️ Análisis de Seguridad de Snyk
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }} 
        with:
          args: --all-projects --sarif --severity-threshold=high
          
      - name: ⬆️ Subir resultados de Snyk (SAST/SCA) a GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk.sarif
        continue-on-error: true

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      
    steps:
      - name: ⬇️ Checkout del Código
        uses: actions/checkout@v4

      - name: 🔑 Login en GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.DOCKER_REGISTRY }}
          # ✅ CORREGIDO: Usar tu usuario de GitHub
          username: ${{ env.DOCKER_USER }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🛠️ Construir y etiquetar imagen Docker
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # ✅ CORREGIDO: Estructura correcta para GHCR
          tags: |
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_USER }}/${{ env.DOCKER_IMAGE_NAME }}:latest
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_USER }}/${{ env.DOCKER_IMAGE_NAME }}:${{ github.sha }}

      - name: ☁️ Despliegue (placeholder)
        run: echo "La imagen ${{ env.DOCKER_IMAGE_NAME }} ha sido publicada en ${{ env.DOCKER_REGISTRY }}"