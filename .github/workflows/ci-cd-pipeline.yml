name: CI/CD Microservicio Node.js

# Se ejecuta con cada push o Pull Request a la rama 'main'
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  
env:
  DOCKER_IMAGE_NAME: mi-microservicio-node
  DOCKER_REGISTRY: ghcr.io/${{ github.repository_owner }}

jobs:
  # ----------------------------------------
  # JOB 1: CI - Pruebas y Seguridad (IE2, IE3)
  # ----------------------------------------
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: ⬇️ Checkout del Código
      uses: actions/checkout@v4

    # IE2: Configuración de Node.js para las pruebas
    - name: ⚙️ Configurar Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 📦 Instalar dependencias
      run: npm install
    
    - name: 🧪 Ejecutar Pruebas Unitarias con Jest (IE2)
      # El pipeline falla si las pruebas unitarias fallan
      run: npm test

    # 🛡️ IE3: Escaneo de Seguridad y Análisis de Código con Snyk (CLI - Método Robusto)
    # Se utiliza la CLI de Snyk para forzar la autenticación y escaneo.
    - name: 🛡️ Escaneo de Seguridad con Snyk (IE3)
      # Se fuerza la ejecución con 'bash' en el runner
      run: |
        npm install -g snyk@latest # Instala la última versión de la CLI de Snyk
        snyk auth ${{ secrets.SNYK_TOKEN }} # Autentica con el secreto de GitHub
        snyk test --severity-threshold=high # Ejecuta el escaneo de seguridad
      env:
        # Se requiere el secreto para la autenticación
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }} 
      
  # ----------------------------------------
  # JOB 2: CD - Construcción y Despliegue (IE1, IE4)
  # Se ejecuta SÓLO si el job de pruebas y seguridad es exitoso
  # ----------------------------------------
  deploy:
    needs: build-and-test # Depende del éxito de las pruebas/seguridad
    runs-on: ubuntu-latest
    
    # Permisos necesarios para subir la imagen al Container Registry de GitHub
    permissions:
      contents: read
      packages: write
      
    steps:
    - name: ⬇️ Checkout del Código
      uses: actions/checkout@v4

    # IE1: Construcción y Push de Imagen Docker a GHCR
    - name: 🔑 Login en GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.DOCKER_REGISTRY }}
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 🛠️ Construir y Etiquetar Imagen (IE1)
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}:latest
        
    # IE4/IE5: Despliegue automático (Ejemplo de entorno simulado con Docker Compose)
    # Descomentar y configurar si se usa SSH para despliegue automático.
    # - name: ☁️ Despliegue a Entorno Simulado (IE4/IE5)
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.DEPLOY_HOST }}
    #     username: ${{ secrets.DEPLOY_USER }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     script: |
    #       # Comandos para desplegar la nueva imagen en un servidor con Docker Compose
    #       cd /ruta/de/despliegue
    #       docker pull ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_IMAGE_NAME }}:latest
    #       docker compose up -d